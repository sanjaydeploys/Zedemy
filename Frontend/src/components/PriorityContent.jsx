import React from 'react';

// This component is a placeholder to maintain compatibility with existing imports.
// The actual critical content is rendered directly by the SSR HTML in #priority-content.
const PriorityContent = () => null;

export default PriorityContent;
